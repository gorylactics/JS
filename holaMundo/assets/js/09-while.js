"use strict"

// WHILE significa mientras que ... se ejecutara este codigo

var year = 2018

while(year <= 2051){
    // mientras que year sea menor o igual que 2051 ejecutate
    console.log("este es el año " + year);
    // para que el loop no sea infito , se debe dar la orden de incrementacion  ++ cosa de que sume desde la base de year hasta llegar hasta el dato que estamamos midiendo
    year++;
}

// y al reves si queremos ir en reversa , debemos usar el operador de decremento --